## 1. 数据模型与样例

- [x] 1.1 定义 `match-details` 响应的忠实 TS 子集（玩家、回合、击杀、装置事件、坐标）。
- [x] 1.2 引入脱敏真实对局样例 `public/sample/competitive-ascent.json` 用于离线开发。
- [ ] 1.3 增加运行时校验/守卫，对缺字段、空 `kills`、非 Bomb 模式（如死斗）优雅降级。
- [ ] 1.4 为 `deriveRoundMoments` 增加单元测试（击杀排序、下包/拆包帧合并、空回合）。

## 2. 地图坐标标定

- [x] 2.1 内置全 12 张图的 displayIcon 与坐标变换参数。
- [x] 2.2 实现 `gameToNormalized/gameToPixel` 变换（游戏 x/y 互换）。
- [x] 2.3 加入 `rotationSteps` 把各图朝向旋转到标准 VCT 方向；Ascent 标定为顺时针 90°。
- [ ] 2.4 逐图核对并标定其余 11 张图的 `rotationSteps`（用各图已知地标坐标验证）。
- [ ] 2.5 为坐标变换 + 旋转增加单元测试（含已知地标的回归用例）。

## 3. 关键帧复盘视图

- [x] 3.1 渲染官方小地图底图 + 全员位置 token + 朝向指示。
- [x] 3.2 渲染击杀连线（击杀者→死者）、死者标记、装置（下包/拆包）标记。
- [x] 3.3 提供回合选择与回合内关键时刻（帧）选择/翻页。
- [ ] 3.4 队伍配色与图例固定化（进攻/防守），并在脱敏与真实数据下都清晰。
- [ ] 3.5 把视图从 `?poc=replay` 开关升级为正式可达入口（与主 App 布局协调）。

## 4. 规格与方向收敛

- [x] 4.1 把沙盘模拟规格 `specs/ability`、`specs/damage` 降级封存至 `specs/_legacy/`。
- [x] 4.2 在 `legacy/sandbox-sim` 分支保留沙盘实现快照并推送远程。
- [ ] 4.3 实现稳定后，评估是否同步精简 `specs/map/spec.md` 中仅服务沙盘的描述。

## 5. 验证

- [x] 5.1 用真实样例实地验证坐标准确性（A 点下包落在 A 区）与地图朝向（CT 上 / T 下）。
- [ ] 5.2 通过 lint + 类型检查 + 相关单元测试。
- [ ] 5.3 在多张地图、多种回合结局（爆炸/拆除/团灭/超时）上回归关键帧渲染。

## 6. 后续变更（非本变更范围，仅登记）

- [ ] 6.1 真实数据接入：官方 Production Key（合规）或本地客户端 token（灰色地带）二选一。
- [ ] 6.2 视觉迭代：token 换英雄头像、击杀热力图、回合经济条、A/B 进攻方向统计。
