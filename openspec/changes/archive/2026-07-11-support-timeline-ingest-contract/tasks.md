## 1. Timeline Source Contract

- [x] 1.1 Add a testable builder for a complete Bilibili `SourceVideo[]` timeline output, including BVID-derived URL and credit.
- [x] 1.2 Extend the timeline CLI with an explicit complete-source mode and required/optional metadata arguments while preserving segments-only output by default.

## 2. Validation and Documentation

- [x] 2.1 Add regression tests for valid complete output, BVID-derived URL, missing/invalid metadata, and legacy segments-only output.
- [x] 2.2 Document both timeline command modes and the fields required for an ingest-ready source file.
- [x] 2.3 Run focused tests, source validation, and package type checking; update the task checklist.
